<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nhnacademy.jdbc.board.member.mapper.MemberMapper">
    <select id="selectMemberByMemberId" resultType="com.nhnacademy.jdbc.board.member.domain.Member">
        select *
        from member
        where member_id = #{memberId}
    </select>

    <select id="selectMemberByMemberNum" resultType="com.nhnacademy.jdbc.board.member.domain.Member">
        select *
        from member
        where member_num = #{memberNum}
    </select>

</mapper>
